<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.EmpMapper">

	<!-- 실습 1 시작 -->
	<insert id="createPost" parameterType="kr.or.ddit.vo.EmpVO">
		<selectKey keyProperty="empNum" resultType="int" order="BEFORE">
			SELECT NVL(MAX(EMP_NUM),0) + 1 FROM EMP
		</selectKey>
		INSERT INTO EMP (EMP_NUM, EMP_NAME, EMP_ADDR, EMP_PHONE, SAL)
			VALUES (#{empNum}, #{empName}, #{empAddr}, #{empPhone}, #{sal})
	</insert>
	<!-- 실습 1 끝 -->
</mapper>