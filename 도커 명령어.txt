도커 접속
1. cd C:\chs_project_20260122_key (key 파일이 있는 디렉토리로 이동)
ssh -i "ssh-key-2026-01-22.key" ubuntu@168.107.51.143

스프링 로그 실시간 확인 (에러 여부 체크):
docker logs -f spring-container

파이썬 로그 실시간 확인
docker logs -f python-container

express.js 로그 실시간 확인
docker logs express-container

파이썬 API 직접 테스트 (서버 내부에서):
curl http://localhost:8000/stock/005930